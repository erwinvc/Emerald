#pragma once

//class ChunkManager
//{
//private:
//	vector<Chunk> m_chunks;
//public:
//    Chunk& AddChunk(const Chunk& chunk);
//
//	const Chunk& GetChunk(const glm::ivec3& chunk);
//	const Chunk& GetChunk(const glm::ivec3& chunk) const;
//
//    uint8 GetVoxel(const glm::ivec3& voxelPosition) const;
//    void SetVoxel(const glm::ivec3& voxelPosition, uint8 voxel);
//    bool HasChunk(const glm::ivec3& chunk) const;
//    bool HasNeighbours(const glm::ivec3& chunkPosition) const;
//    void EnsureNeighbours(const glm::ivec3& chunkPosition);
//
//    const vector<Chunk>& chunks() const;
//
//
//};